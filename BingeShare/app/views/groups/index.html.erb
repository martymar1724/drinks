<div class="row">

    <% @groups.each do |t| %>
        <div class="col-lg-3 col-xs-6">
        <div class="small-box bg-aqua">
        <div class="inner">
        	<h3><%= t.name %></h3>
        	<p><%= t.description %></p>
        	<p><%= t.created_at %></p>
        	
        	
        </div>

        <div class="icon">
        	<!-- Place icon here
            <i class="fa fa-tasks"></i>
            -->
        </div>	
	<%= link_to " See services", group_path(t), class: "small-box-footer fa fa-minus-circle" %>    	
		</div>	
    	</div>
    <% end %>